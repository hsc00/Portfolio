function checkSign(num) {
  /*Use multiple conditional operators in the 
  checkSign function to check if a number is 
  positive, negative or zero. */
  return num == 0 ? "zero" : num > 0 ? "positive" : "negative";
}

checkSign(10);
